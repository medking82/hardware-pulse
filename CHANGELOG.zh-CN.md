# 更新记录

[English](CHANGELOG.md)

日期为发布日期。未发布条目仅描述源代码变化，不代表已有可下载版本。作者：[Marck Wong](https://github.com/medking82)。

## 未发布

- 跨平台桌面设置新增即时生效并保存的文字大小，以及与浮动窗口同步的置顶开关。模糊回退列表补充 AcrylicBlur，使当前 macOS 后端可使用原生窗口后方模糊材质。

- 跨平台桌面设置新增可保存的系统背景模糊开关，显示实际生效或回退状态。不支持时保留可用背景效果。当前固定版本的 Avalonia macOS 后端提供原生模糊，正在补充原生视图验证；尚未实现模糊半径调节。

- 跨平台浮动监控窗口锁定后隐藏不可操作的编辑控件，从主界面或托盘重新打开后恢复控件，保持窗口尺寸不变。

- 跨平台版新增独立桌面设置页，可保存浮动监控背景的 0–100% 不透明度；文字保持不透明。不支持透明效果的平台使用纯色背景，此设置不提供模糊效果。

- 跨平台浮动监控窗口复用已开启的 Codex 额度数据，显示全部额度窗口，跟随语言切换，关闭后清除读数，不增加请求或计时器。

- 跨平台浮动监控窗口在独立配置中保存大小、位置及置顶选择。重新打开时仍可编辑，屏幕外的位置会恢复到可用显示器范围内。

- Linux X11 浮动窗口新增基于 XFixes 的鼠标穿透锁定，解锁时恢复默认输入区域及窗口边框。x64/ARM64 原生检查已通过，尚未实现原生 Wayland 锁定。

- 跨平台界面的 macOS 浮动窗口新增 AppKit 鼠标穿透锁定，复用主界面及菜单栏解锁入口。Intel/ARM64 生命周期及安装包检查均已通过。

- 跨平台界面的 Windows 浮动监控窗口新增原生鼠标穿透锁定；从主界面或托盘重新打开即可解锁同一窗口。Windows ARM64 交互验证尚待完成。

- 跨平台托盘或菜单栏现在可以重新打开已有的浮动监控窗口，入口跟随应用语言，退出后旧菜单操作不会再次打开窗口。

- 跨平台界面新增浮动监控窗口，支持置顶，并复用现有 CPU、内存、网络和传感器读数，不增加采样任务。重复打开会返回同一窗口，语言和主题跟随主窗口，退出主窗口时一并关闭。跨平台界面尚未实现桌面层嵌入和 FPS。

- 跨平台桌面版内嵌固定版本的 Noto Sans CJK 简体和繁体字体，解决未安装中文字体的系统上界面缺字的问题。构建时校验文件大小和 SHA-256，安装包附带 OFL 许可；原生启动检查会将界面文案缺字判为失败。无需安装系统字体，也不会在运行时下载字体。

- 共享桌面应用支持自动跟随系统、英语、简体中文和繁体中文，覆盖监控、设置、传感器状态、Codex 额度与托盘操作。语言即时切换，不重启采样或重新读取登录信息，并保存到独立的预览版设置中。自动模式会区分中文书写体系和地区。

## 0.6.27 — 2026-09-16

- Windows 10/11 维护更新：原生 DPI 查询不可用或返回零时，窗口吸附可使用 WPF DPI 回退。
- FPS 和局部对比度入口增加明确的系统能力检查，保留已保存的偏好设置。新版和旧版系统分别校验对应更新安装包，现代 Windows 安装包文件名及完整性检查保持不变。
- 保留 Windows 10/11 硬件读数、FPS 采集、桌面模式和应用图标配色。本次发布不将 macOS/Linux 预览版提升为正式版，也不代表已验证 Win7 兼容性。

## 0.6.26-win7-preview.1 — 2026-09-16

- 发布独立的 Windows 7 SP1 x64 实机测试安装包，需要 .NET Framework 4.8，提供无需驱动的 CPU、内存和网络读数。不支持 FPS 和局部对比度，温度、风扇和 GPU 读数暂不可用，目标系统验证仍待完成。

## 0.6.26 — 2026-09-16

- Windows 桌面模式下，即使选定的图标颜色与文字颜色相同，也会保留 App 配色；局部自适应对比度不会再误判为单色模式。
- 置顶时同样尊重“跟随应用颜色”选项，关闭后才采用自适应黑白图标。

## 0.6.25 — 2026-09-16

- 修复桌面模式置顶或开启局部自适应对比度后，图标失去应用配色的问题。选择跟随应用颜色时保留原色，通过细微的反色描边辅助辨认。
- 文字仍可自动调整对比度；关闭跟随应用颜色后，图标仍可切换黑白。截图模式继续保留当前外观。

## 0.7.0-preview.1 — 2026-09-16

首次发布面向 Windows、Linux、macOS 的 x64／ARM64 实验性桌面版本，共六个软件包。原生持续集成检查、软件包文件清单和发布后的 SHA-256 检查通过；尚未与 Windows 稳定版实现完整功能对等。稳定版 Windows 更新通道保持 v0.6.24，预览版使用独立设置。

## 0.6.24 — 2026-09-15

FPS 统计改为复用有界缓冲区，减少每次读取的临时内存分配。数值语义、历史记录范围和刷新间隔保持不变；.NET Framework/.NET 10 与原生回归测试通过。基准测试仅代表固定 FPS 工作负载。

## 0.6.23 — 2026-09-15

Windows 硬件采样进入适配器；数值格式、额度解析、FPS 统计、设置值规则与背景材质策略进入共享核心。.NET Framework/.NET 10 共享测试和完整原生回归测试通过。现有设置保持兼容；跨平台运行程序与完整应用程序性能对照测试尚未完成。

## 0.6.22 — 2026-09-15

共享读数、额度刷新生命周期、网络数据格式与对比度分析进入核心；Windows 数据/网络适配器独立成 DLL。现有 Windows 应用程序、设置与刷新行为保持兼容，.NET Framework/.NET 10 核心测试和完整回归检查通过。其他平台尚未完成适配。

## 0.6.21 — 2026-09-15

开启局部对比度后，所有桌面 SVG 图标按自身位置的背景独立调整明暗。应用程序配色保留色相；关闭配色时使用黑白对比色，复杂背景加入细轮廓。截图模式保持配色冻结。

## 0.6.20 — 2026-09-15

新增设置 → 常规 → 导出诊断，保存包含机型、版本、传感器读数与风扇映射的 JSON，方便排查风扇漏识别。不包含账号凭据、网络标识或截图，也不会自动上传。

## 0.6.19 — 2026-09-15

温度、CPU/GPU 使用率、RAM/VRAM 容量与使用率固定保留一位小数，整数也显示末尾 .0，例如 54.0 °C、42.0%。FPS 与风扇 RPM 继续使用整数。

## 0.6.18 — 2026-09-15

降低局部对比度的画面采集、分析与内存分配开销，拆出独立 Pulse.Core 组件。受控组件基准测试中，每帧 CPU 时间约降低 87%，托管内存分配约降低 93%；RAM 与完整游戏场景尚未证明改善。ARM64、Linux、macOS 应用程序支持尚未实现。

## 0.6.17 — 2026-09-15

修复桌面背景不透明度为 0% 时无法拖动的问题。未锁定编辑界面使用极低透明度的输入区域；已锁定后恢复真正透明，保留用户保存的不透明度。

## 0.6.16 — 2026-09-15

修复正式桌面层解锁后仍保留不激活窗口的样式的问题。带真实层的测试已确认拖动和按钮都正常；锁定后仍保持鼠标穿透。

## 0.6.15 — 2026-09-15

再次修复桌面拖动，测试窗口已记录连续位置变化。修复旧交换链历史数据压过新数据流、导致 FPS 错误显示 — 的情况。

## 0.6.14 — 2026-09-15

修复未锁定桌面内容区域无法拖动。按钮和滚动条保留操作，边缘继续调整大小，结束移动后保存位置。

## 0.6.13 — 2026-09-15

FPS SVG 使用 Pulse 薄荷青；局部对比度保留色相并调整亮度。复杂背景下加入细反色轮廓，简单背景保持清晰字形，无需增加面板背景。

## 0.6.12 — 2026-09-15

桌面 FPS 合并为一行，加入 NOW / AVG / MIN 小号角标。局部对比度按完整标签和读数调色；新增 15 秒截图模式，支持 Win+Shift+S。锁定的桌面自动容纳内容，减少不必要的滚动。

## 0.6.11 — 2026-09-15

桌面 FPS 分开显示当前、平均与最低值；没有帧时三项只显示 —，不再显示等待帧数据导致换行。平均和最低使用最近 60 秒滚动统计。

## 0.6.10 — 2026-09-15

新增可选的局部文字自适应对比度，黑白配色按像素位置变化，SVG 配色独立。此模式可能从截图及录屏中排除桌面面板。修复 0% 背景残留外框与文字颜色无法选择；桌面读数可单独开启 FPS，无需独立悬浮层。HDR 与高速游戏效果仍待实测。

## 0.6.9 — 2026-09-15

改进拖曳换列与渐进吸附，桌面支持附近应用程序吸附，锁定后提示从托盘解锁。明确中英文字体回退方案，并开放背景和文字不透明度 0%–100%。

## 0.6.8 — 2026-09-15

设置按功能分组，桌面布局、外观和显示项目集中到同一页，宽窗口支持多列。置顶使用浅烟灰背景，默认不透明度 55%；普通桌面和置顶背景可分别调整，文字保持清晰，鼠标穿透不变。桌面模糊强度尚未实现。

## 0.6.7 — 2026-09-15

桌面新增默认关闭的“置顶显示”。默认仍在壁纸 / Wallpaper Engine 上方、普通应用程序下方；开启后浮在应用程序上方，锁定时鼠标穿透，编辑时接受鼠标操作。独占全屏游戏仍待实测。

## 0.6.6 — 2026-09-15

桌面的 LAN、Wi-Fi 连接速率和信号使用独立 SVG。新安装默认背景不透明度 30%、#35383B、开启系统毛玻璃；已有设置保持不变。GitHub 主视觉图使用实际 WPF 渲染和演示数据。

## 0.6.5 — 2026-09-15

应用程序和桌面独立显示 LAN / Wi-Fi 连接速率及 Wi-Fi 信号。Wi-Fi 空闲时仍更新连接速率；断线清除旧数值，优先匹配物理适配器。

## 0.6.4 — 2026-09-15

再次点击应用程序快捷方式会唤回现有首页，支持隐藏、最小化和桌面模式；复用现有应用程序与采集器。

## 0.6.3 — 2026-09-15

修复 Codex 额外额度遗漏；托盘双击和显示 Pulse 直接恢复应用程序首页，保留桌面面板。

## 0.6.2 — 2026-09-15

设置支持自适应多列、统一控件和独立应用程序/桌面外观；应用程序额度可显示完整额度并拖曳排序。桌面可直接从边缘调整大小、在面板内锁定，并从托盘重新编辑；保存的桌面模式在自动启动时直接显示，避免主窗口闪现。

## 0.6.1 — 2026-09-15

- 桌面名称使用整行可用宽度；窄行自动换行，不再遮挡数值。
- 在首页设置按钮旁显示更新、下载进度、重试及安装入口。
- 无需展开自定义桌面设置即可切换图标配色。

## 0.6.0 — 2026-09-15

- 应用卡片适配一至三列，支持动画排序和键盘操作。
- 从监控页面进入桌面模式，独立设置读数的显示与顺序。
- 编辑时可调整桌面面板大小，选择自动列数或列数上限，并保存位置与大小。
- 集中桌面外观设置，新增推荐样式；使用统一圆角背景代替各文字独立的对比色背景块。
- 桌面 SVG 图标可跟随应用配色。
- 同步监控页面、设置和系统托盘的 FPS 开关，并使用 SVG 图标。
- 桌面显示连接类型和协商连接速率；仅在 Windows 提供数据时显示 Wi-Fi 信号百分比，不扫描 Wi-Fi 或更改网络连接。

## 0.5.12 — 2026-09-15

- Antigravity 仅显示 Gemini 的 5 小时与每周额度，移除 Claude/GPT 额度池以及重复或无关条目。
- 桌面使用更短的 Gemini 标签，监控卡片注明 Gemini 分组。
- 已禁用或缺失的额度保持不可用；顺序固定为 5 小时、每周。

## 0.5.11 — 2026-09-15

- 修复 Antigravity 已运行却读不到额度的问题，Codex 精简为主每周额度。无需修改 CLI 自启动或登录配置。

## 0.5.10 — 2026-09-15

- 新增 AI 额度、SVG 图标和网络连接速率。Codex、Claude 实机读取检查已通过；Antigravity 实机读取检查待运行其应用程序后验证。

## 0.5.9 — 2026-09-14

- 修复桌面对比度与贴边留白，新增图标/温度配色选项，以及支持单位切换的上下行网速。无需新采集器或 PowerShell 运行环境。

## 0.5.8 — 2026-09-14

- 桌面模式先进入编辑预览，完成后锁定；设置支持独立读数动画拖曳排序与键盘调整，并补齐四边边缘吸附。

## 0.5.7 — 2026-09-14

- 新增桌面模式：数据显示在壁纸上、普通应用程序下方。支持独立字号、颜色、间距、位置和锁定后鼠标穿透，复用现有采集器。

## 0.5.5 — 2026-09-14

- 修复 FPS 实际读取与自动跟随，保留悬浮层设置并支持应用重启后重连。新增状态条背景颜色及透明度设置，文字清晰度不受影响。

## 0.5.4 — 2026-09-14

- 修复过期／离线时硬件型号退回默认名称，以及切换语言后动态状态保留旧语言的问题。保留过期提示，不把旧读数显示成实时数据。

## 0.5.3 — 2026-09-14

- 将快照/会话状态从 Shell 独立出来，新增不依赖 WPF 的测试；保留原有 UI 和生命周期行为。包含在 0.5.3 维护发行版。

## 0.5.2 — 2026-09-14

- 复用传感器发现 Regex 和界面画刷，减少托管内存分配；隐藏或最小化时暂停卡片渲染，继续记录本次峰值和过期状态，恢复窗口时立即刷新。保留原有采样频率及传感器选择。

## 0.5.1 — 2026-09-14

- 修复缺失任务被 .NET 转为 FileNotFoundException 后中断启动任务注册的问题；直接覆盖安装即可修复失败的 0.5.0 安装，保留偏好设置。
- 新增真实任务计划程序回归测试和隔离集成测试；不改变权限或硬件采集，安装程序仍为自签名。

## 0.5.0 — 2026-09-14

- 已安装的运行程序改为 C#/WPF，不再依赖 PowerShell；保留设置、桌面位置、卡片选择、语言、传感器发现、悬浮层和更新器。
- 安装程序支持原位升级，清理明确列出的旧应用程序文件，保留 Windows 组件、共享 PawnIO 和用户设置。修复任务计划程序省略默认 XML 值时的兼容性。
- 对比测试显示本机内存占用降低；基准测试不是所有电脑或电池续航的保证。SignPath 仍待审核，安装程序仍为自签名。

## 0.4.9 — 2026-09-14

- 紧凑使用紧凑读数；明细保留完整数据并自适应间距，增加已选中状态。固定滚动前后的卡片宽度，采用细滚动条与圆角键盘焦点。
- 补齐水泵与系统风扇标签翻译，说明单通道 0 RPM 的含义；未修改驱动或硬件采集逻辑。关闭未使用的悬浮层计时器，仍依赖 PowerShell 运行环境。

## 0.4.8 — 2026-09-14

- 外观新增 10–16 px 字号设置，默认 12，自动保存并兼容旧大字号设置；使用 WPF 逻辑像素，跟随 Windows 显示缩放。
- 卡片随字号调整字体排版与间距，窄窗口下标题和紧凑读数自动换行，长标签省略并通过提示说明显示全文，避免标题重叠与多余高度。
- 已验证字号保存、重启恢复、240/310 DIP 标题边界与原有布局/语言回归测试；大字号或明细仍可能需要滚动。

## 0.4.7 — 2026-09-14

- 通用识别 Intel D3D 3D 负载及 GPU 共享内存，不绑定电脑型号；共享内存与独立 VRAM 明确区分。
- 监控页面根据传感器能力隐藏缺失的风扇、温度和硬盘占位，保留卡片设置，传感器恢复后自动显示。无数据不再误认为零读数。
- 移除固定 17 项分母。此版本没有新增厂商专用风扇控制器支持。

## 0.4.6 — 2026-09-14

- 修复 Windows 脚本策略导致的应用程序/启动辅助程序/采集器启动失败：仅当前进程使用 RemoteSigned，不修改持久系统设置，保留组策略与 Internet 脚本签名检查。
- 窄窗口不再整体缩字；使用 Display 字体排版和紧凑卡片间距。

## 0.4.5 — 2026-09-14

- 修复启动时无法删除旧 STOP 文件后忽略新退出请求的问题，避免组件隐藏到托盘后继续占用安装文件。

## 0.4.4 — 2026-09-14

- 修复缓慢拖离边缘时反复吸回的问题：根据拖动起点累计光标位移，正常拖动即可释放，无需 Alt；保留 24 DIP 吸附范围。

## 0.4.3 — 2026-09-14

- 设置改为折叠分组与开关；修正齿轮按钮居中、焦点和窄窗口标签换行。
- 修复非活动状态透明度，卡片/渐变跟随不透明度；锁定监控页面更透明，禁止移动、调整大小和排序，托盘保留解锁入口。
- 吸附范围提高至 24 DIP，拖动时对齐屏幕/窗口边缘，保留 Alt 自由移动。
- 应用程序增加自动（跟随系统），安装程序支持三种语言；新增应用程序内下载、校验和点击安装更新。

## 0.4.2 — 2026-09-13

- 补齐动态硬件说明、进出风、内存槽位及用量标签的简繁体翻译；“显存结温”改为“显存温度”，提示说明解释其含义，型号与自定义名称保持原样。

## 0.4.1 — 2026-09-13

- 自动信息密度根据实际可视区域测量：优先完整信息，依次压缩留白、调整指标排列，最后才隐藏设备说明。窗口放大后自动恢复；明细切换增加轻量过渡并尊重减少动态效果。

## 0.4.0 — 2026-09-13 （预发布版）

- 新增游戏悬浮层六位置、紧凑/详细布局、可选指标、PresentMon FPS；AVG/MIN/1% Low 使用最近 60 秒，当前值使用最近 1 秒。
- 新增关闭后收起到托盘、明确的退出入口、开机启动复选框、背景颜色、0–100% 不透明度和可选更新检查。
- 安装程序增加覆盖前停止旧版流程；原创代码采用 MIT，SignPath 尚未获批；展示图使用虚构数据。
- WPF 回归测试已通过；移除独立升级辅助程序后，安装程序在本机成功覆盖安装（退出码 0，无需重启），未修改 Bitdefender 防护设置，已验证实时读数。真实游戏、高权限 FPS 和全新环境场景待验证。当前更新操作打开下载页面，不会自动安装。

## [0.3.1] — 2026-09-13

- 卡片拖动增加轻微抬起、相邻卡片让位，以及 220 ms 缓出回落；可在动画中重新抓取。
- Esc 取消后恢复原顺序且不保存；放下完成后仅保存一次。
- 遵循 Windows 动画偏好；键盘菜单排序保持即时响应。
- WPF 手势回归测试覆盖预览、取消、减少动态效果、提交和中断回落；真实指针手感和边缘自动滚动待验证。
- 尚未修复升级时无法自动关闭高权限采集器的问题。

## [0.3.0] — 2026-09-13

- 新增英语、简体中文、繁體中文 UI，支持即时切换和重启后恢复；保留硬件型号、自定义名称及卡片顺序。
- 增强 .NET / PowerShell 前置条件检查和 PawnIO 安装后检查。
- README 与发行说明改为英语优先；增加版本一致性检查。
- 语言、持久保存、窄窗口和现有回归检查通过；Windows 10 / 全新环境 / 升级未在发行版前验证。之后发现运行中的采集器可阻塞升级，停止采集器可释放占用。

## [0.2.0] — 2026-09-13

- 首个公开发行版：Windows EXE 安装程序，包含固定版本的依赖项、第三方声明与源代码归档与校验值。
- 提供紧凑毛玻璃 UI、CPU/GPU/内存/NVMe/风扇读数、RAM/VRAM 用量、独立设置、自定义名称和自动发现。
- 保存窗口、外观、卡片顺序；支持拖动排序、键盘菜单和窗口边缘吸附。
- 修复过期快照、粘滞拖动、UTF-8 编码、齿轮按钮图标与 GPU 风扇标签问题；GUI 自动启动改用独立任务。
- 已验证 Windows 11 当前机器；其他品牌主要依靠模拟数据，其他操作系统/全新环境/重启场景仍有验证限制。

[0.3.1]: https://github.com/medking82/hardware-pulse/releases/tag/v0.3.1
[0.3.0]: https://github.com/medking82/hardware-pulse/releases/tag/v0.3.0
[0.2.0]: https://github.com/medking82/hardware-pulse/releases/tag/v0.2.0
