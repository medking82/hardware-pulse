# 依赖项维护说明

[English](DEPENDENCIES.md)

## 简体中文维护说明
依赖项使用固定发行版本和 SHA-256；用户机器不会持续 `git pull` 或自动替换上游依赖。更新在开发环境完成，经过传感器、退出、安装程序升级和相关 FPS 回归测试验证后，随 Hardware Pulse 新版本发布。保留上游许可证、必要源代码和签名。
Linux / macOS 属于计划支持，需要各自的后端和 UI 适配；当前不宣称支持。自动上游更新检查 / PR 工作流程尚未实现。
