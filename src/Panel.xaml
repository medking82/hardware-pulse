<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="Hardware Pulse Glass" Width="280" Height="650" MinWidth="240" MinHeight="340" WindowStyle="None" ResizeMode="CanResizeWithGrip" Background="#182332" Foreground="#F0F5FA" FontFamily="Segoe UI" FontSize="12" TextOptions.TextFormattingMode="Display" UseLayoutRounding="True" SnapsToDevicePixels="True" WindowStartupLocation="CenterScreen">
  <Window.Resources>
    <Style x:Key="PulseScrollThumb" TargetType="Thumb">
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Thumb">
            <Grid Background="Transparent">
              <Border x:Name="ThumbBody" Background="#607E94A5" CornerRadius="3" Margin="3,0" />
            </Grid>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="ThumbBody" Property="Background" Value="#B098B0C0" />
              </Trigger>
              <Trigger Property="IsDragging" Value="True">
                <Setter TargetName="ThumbBody" Property="Background" Value="#D0ADD9DF" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="PulseScrollPage" TargetType="RepeatButton">
      <Setter Property="Focusable" Value="False" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="RepeatButton">
            <Border Background="Transparent" />
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="ScrollBar">
      <Setter Property="Width" Value="12" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ScrollBar">
            <Grid Background="Transparent" Margin="0,3">
              <Track x:Name="PART_Track" IsDirectionReversed="True" Orientation="{TemplateBinding Orientation}" Minimum="{TemplateBinding Minimum}" Maximum="{TemplateBinding Maximum}" ViewportSize="{TemplateBinding ViewportSize}" Value="{Binding Value, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                <Track.DecreaseRepeatButton>
                  <RepeatButton Command="ScrollBar.PageUpCommand" Style="{StaticResource PulseScrollPage}" />
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                  <Thumb Style="{StaticResource PulseScrollThumb}" MinHeight="24" />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                  <RepeatButton Command="ScrollBar.PageDownCommand" Style="{StaticResource PulseScrollPage}" />
                </Track.IncreaseRepeatButton>
              </Track>
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="TextBlock">
      <Setter Property="TextWrapping" Value="Wrap" />
    </Style>
    <Style TargetType="Button">
      <Setter Property="FocusVisualStyle">
        <Setter.Value>
          <Style TargetType="Control">
            <Setter Property="Template">
              <Setter.Value>
                <ControlTemplate>
                  <Border Margin="-3" CornerRadius="18" BorderThickness="1" BorderBrush="#BFEAF9" />
                </ControlTemplate>
              </Setter.Value>
            </Setter>
          </Style>
        </Setter.Value>
      </Setter>
      <Setter Property="Foreground" Value="#F3F7FC" />
      <Setter Property="Background" Value="#223B536B" />
      <Setter Property="Padding" Value="10,5" />
      <Setter Property="Margin" Value="0,0,6,0" />
      <Setter Property="MinHeight" Value="28" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Plate" Background="{TemplateBinding Background}" BorderBrush="#496F829A" BorderThickness="1" CornerRadius="15" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Plate" Property="Background" Value="#505E829D" />
              </Trigger>
              <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter TargetName="Plate" Property="BorderBrush" Value="#BFEAF9" />
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Plate" Property="Background" Value="#8078A5B9" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="CheckBox">
      <Setter Property="Foreground" Value="#D9E7F1" />
      <Setter Property="VerticalContentAlignment" Value="Center" />
      <Setter Property="Padding" Value="3" />
      <Setter Property="Margin" Value="0,0,10,0" />
      <Setter Property="MinHeight" Value="32" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="CheckBox">
            <Grid Background="Transparent">
              <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="44" /></Grid.ColumnDefinitions>
              <TextBlock Text="{TemplateBinding Content}" Margin="0,4,10,4" VerticalAlignment="Center" TextWrapping="Wrap" />
              <Border x:Name="SwitchTrack" Grid.Column="1" Width="38" Height="22" CornerRadius="11" Background="#52606D" BorderBrush="#8797A6" BorderThickness="1" VerticalAlignment="Center">
                <Ellipse x:Name="SwitchKnob" Width="16" Height="16" Fill="#F0F5FA" HorizontalAlignment="Left" Margin="2,0" />
              </Border>
            </Grid>
            <ControlTemplate.Triggers>
              <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="SwitchTrack" Property="Background" Value="#A6D8C8" />
                <Setter TargetName="SwitchKnob" Property="Fill" Value="#182332" />
                <Setter TargetName="SwitchKnob" Property="HorizontalAlignment" Value="Right" />
              </Trigger>
              <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="SwitchTrack" Property="BorderBrush" Value="#FFFFFF" /><Setter TargetName="SwitchTrack" Property="BorderThickness" Value="2" /></Trigger>
              <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.5" /></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="Expander">
      <Setter Property="Foreground" Value="#F0F5FA" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Expander">
            <Border BorderBrush="#486B7C89" BorderThickness="0,0,0,1" Padding="0,0,0,8">
              <StackPanel>
                <ToggleButton x:Name="SectionHeader" IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" Foreground="{TemplateBinding Foreground}" MinHeight="40" HorizontalContentAlignment="Stretch">
                  <ToggleButton.Template>
                    <ControlTemplate TargetType="ToggleButton">
                      <Border x:Name="HeaderPlate" Background="Transparent" CornerRadius="8" Padding="8,6">
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="20" /></Grid.ColumnDefinitions>
                          <ContentPresenter VerticalAlignment="Center" />
                          <Path x:Name="Chevron" Grid.Column="1" Data="M 0 0 L 5 5 L 10 0" Stroke="{TemplateBinding Foreground}" StrokeThickness="1.5" HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5" />
                        </Grid>
                      </Border>
                      <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True"><Setter TargetName="Chevron" Property="RenderTransform"><Setter.Value><RotateTransform Angle="180" /></Setter.Value></Setter></Trigger>
                        <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="HeaderPlate" Property="Background" Value="#185F8799" /></Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="HeaderPlate" Property="BorderBrush" Value="#BFEAF9" /><Setter TargetName="HeaderPlate" Property="BorderThickness" Value="1" /></Trigger>
                      </ControlTemplate.Triggers>
                    </ControlTemplate>
                  </ToggleButton.Template>
                  <ContentPresenter Content="{TemplateBinding Header}" TextElement.FontWeight="SemiBold" />
                </ToggleButton>
                <ContentPresenter x:Name="SectionBody" Content="{TemplateBinding Content}" Visibility="Collapsed" Margin="8,10,8,8" />
              </StackPanel>
            </Border>
            <ControlTemplate.Triggers><Trigger Property="IsExpanded" Value="True"><Setter TargetName="SectionBody" Property="Visibility" Value="Visible" /></Trigger></ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>
  <Grid x:Name="Viewport">
    <Grid.Background>
      <RadialGradientBrush Center="0.1,0" GradientOrigin="0,0" RadiusX="1.3" RadiusY="1">
        <GradientStop Color="#404C9DAD" Offset="0" />
        <GradientStop Color="#05152136" Offset="0.6" />
        <GradientStop Color="#302C3C68" Offset="1" />
      </RadialGradientBrush>
    </Grid.Background>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <StackPanel Margin="14,10,14,8">
      <Grid>
        <Border x:Name="DragHandle" Background="Transparent" Margin="0,0,90,0" Cursor="SizeAll">
          <StackPanel Orientation="Horizontal">
            <ContentControl x:Name="BrandIcon" VerticalAlignment="Center" Margin="0,0,7,0" />
            <TextBlock Text="Pulse" FontSize="22" FontWeight="SemiBold" />
          </StackPanel>
        </Border>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
          <Button x:Name="Minimize" Content="−" ToolTip="Minimize" Padding="10,3" />
          <Button x:Name="Close" Content="×" ToolTip="Close widget" Padding="10,3" Margin="0" />
        </StackPanel>
      </Grid>
      <StackPanel x:Name="MonitorControls" Orientation="Horizontal" Margin="0,10,0,8">
        <Button x:Name="Live" Content="Live" />
        <Button x:Name="Max" Content="Session Max" />
        <Button x:Name="Details" Content="Details" ToolTip="Show full hardware details" Padding="7,5" Margin="0" />
      </StackPanel>
      <TextBlock x:Name="Status" Text="Connecting to sensors…" Foreground="#ACDFD2" FontSize="10" />
    </StackPanel>
    <ScrollViewer x:Name="CardScroll" Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="12,0,12,0" FocusVisualStyle="{x:Null}">
      <ScrollViewer.Template>
        <ControlTemplate TargetType="ScrollViewer">
          <Border x:Name="ScrollFocus" BorderThickness="1" BorderBrush="Transparent" CornerRadius="14">
            <Grid>
              <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" Margin="12,0,24,0" CanContentScroll="{TemplateBinding CanContentScroll}" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" />
              <ScrollBar x:Name="PART_VerticalScrollBar" HorizontalAlignment="Right" Width="10" Orientation="Vertical" Minimum="0" Maximum="{TemplateBinding ScrollableHeight}" ViewportSize="{TemplateBinding ViewportHeight}" Value="{Binding VerticalOffset, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}" Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" />
            </Grid>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="ScrollFocus" Property="BorderBrush" Value="#608ECBC4" /></Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </ScrollViewer.Template>
      <StackPanel x:Name="Cards" />
    </ScrollViewer>
    <TextBlock x:Name="CardsEmpty" Grid.Row="1" Text="No cards shown. Choose cards in Settings." Visibility="Collapsed" Margin="20" HorizontalAlignment="Center" VerticalAlignment="Center" />
    <ScrollViewer x:Name="SettingsPage" Grid.Row="1" Visibility="Collapsed" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="14,10,14,0">
      <StackPanel>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
          <Button x:Name="Back" ToolTip="Back to Monitor" AutomationProperties.Name="Back to Monitor" Width="32" Padding="7" />
          <TextBlock Text="Settings" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center" Margin="4,0,0,0" />
        </StackPanel>
        <Expander Header="General" IsExpanded="True" Margin="0,0,0,10">
          <StackPanel>
            <TextBlock Text="Language" Margin="0,0,0,6" />
            <ComboBox x:Name="LanguagePicker" AutomationProperties.Name="Language" MinHeight="30" Margin="0,0,0,16" Foreground="#182332" FontFamily="Segoe UI, Microsoft YaHei UI, Microsoft JhengHei UI">
              <ComboBoxItem Tag="auto" Content="Auto (System)" />
              <ComboBoxItem Tag="en" Content="English" />
              <ComboBoxItem Tag="zh-CN" Content="简体中文" />
              <ComboBoxItem Tag="zh-TW" Content="繁體中文" />
            </ComboBox>
            <CheckBox x:Name="StartWithWindows" Content="Start with Windows" Margin="0,0,0,10" />
            <TextBlock x:Name="StartupStatus" FontSize="11" Margin="0,0,0,10" />
            <TextBlock Text="Version 0.5.2 · Marck Wong" FontSize="11" Foreground="#ADC1D2" Margin="0,0,0,8" />
            <Button x:Name="GitHub" Content="GitHub" HorizontalAlignment="Left" ToolTip="github.com/medking82/hardware-pulse" />
            <CheckBox x:Name="AutoUpdates" Content="Check for Updates Automatically" Margin="0,12,0,8" />
            <CheckBox x:Name="AutoDownload" Content="Download Updates Automatically" Margin="0,0,0,8" />
            <TextBlock Text="Checks GitHub for new releases. No hardware data is sent." FontSize="11" Margin="0,0,0,8" />
            <Button x:Name="CheckUpdates" Content="Check for Updates" Margin="0,0,0,8" />
            <Button x:Name="GetUpdate" Content="Download Update" Visibility="Collapsed" Margin="0,0,0,8" />
            <ProgressBar x:Name="DownloadProgress" Minimum="0" Maximum="100" Height="4" Visibility="Collapsed" Margin="0,0,0,8" />
            <Button x:Name="InstallUpdate" Content="Install and Restart" Visibility="Collapsed" Margin="0,0,0,8" />
            <TextBlock x:Name="UpdateStatus" Text="" FontSize="11" />
            <TextBlock Text="Sensors by LibreHardwareMonitor. Shared driver by PawnIO." FontSize="10" Foreground="#ADC1D2" Margin="0,10,0,0" />
          </StackPanel>
        </Expander>
        <Expander Header="Appearance" IsExpanded="False" Margin="0,0,0,10">
          <StackPanel>
            <Grid Margin="0,0,0,8">
              <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <TextBlock Text="Background Opacity" Margin="0,0,8,0" />
              <TextBlock x:Name="OpacityValue" Grid.Column="1" Text="70%" />
            </Grid>
            <Slider x:Name="OpacitySlider" Minimum="0" Maximum="100" Value="70" SmallChange="5" LargeChange="10" AutomationProperties.Name="Background opacity" Margin="0,0,0,18" />
            <CheckBox x:Name="Solid" Content="Solid Background" Margin="0,0,0,14" />
            <CheckBox x:Name="Large" Content="Larger Text" Visibility="Collapsed" Margin="0" />
            <Grid Margin="0,0,0,6">
              <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
              <TextBlock Text="Text Size"/>
              <TextBlock x:Name="FontSizeValue" Grid.Column="1" Text="12 px"/>
            </Grid>
            <Slider x:Name="FontSizeSlider" Minimum="10" Maximum="16" Value="12" TickFrequency="1" IsSnapToTickEnabled="True" SmallChange="1" LargeChange="1" AutomationProperties.Name="Text Size"/>
            <TextBlock Text="Logical pixels; follows Windows display scaling." FontSize="11" TextWrapping="Wrap" Margin="0,6,0,0"/>
            <Button x:Name="BackgroundColor" Content="Background Color" Margin="0,14,0,0" />
            <TextBlock Text="Text contrast adapts to the selected color. Glass blur is controlled by Windows; Solid Background disables it." FontSize="11" Margin="0,8,0,0" />
          </StackPanel>
        </Expander>
        <Expander Header="Window" IsExpanded="False" Margin="0,0,0,10">
          <StackPanel>
            <CheckBox x:Name="Pin" Content="Always on Top" Margin="0,0,0,10" />
            <CheckBox x:Name="LockPosition" Content="Lock Position and Size" Margin="0,0,0,10" />
            <TextBlock Text="Position and size are remembered automatically." FontSize="11" Foreground="#ADC1D2" />
          </StackPanel>
        </Expander>
        <Expander Header="Cards" Foreground="#F0F5FA" Margin="0,16,0,0">
          <StackPanel x:Name="CardOptions" Margin="0,10,0,0" />
        </Expander>
        <Expander Header="Game Overlay" Foreground="#F0F5FA" Margin="0,16,0,0">
          <StackPanel Margin="0,10,0,0">
            <TextBlock Text="Windowed / borderless games. FPS capture requires administrator. Stats use application presents, not generated frames." FontSize="11" Margin="0,0,0,10" />
            <ComboBox x:Name="GamePicker" AutomationProperties.Name="Game" MinHeight="30" Margin="0,0,0,8" Foreground="#182332" />
            <Button x:Name="RefreshGames" Content="Refresh Games" Margin="0,0,0,10" />
            <CheckBox x:Name="OverlayEnabled" Content="Show Overlay" Margin="0,0,0,10" />
            <ComboBox x:Name="OverlayPosition" AutomationProperties.Name="Overlay Position" MinHeight="30" Margin="0,0,0,10" Foreground="#182332">
              <ComboBoxItem Content="Top Left" Tag="top-left" />
              <ComboBoxItem Content="Top" Tag="top" />
              <ComboBoxItem Content="Top Right" Tag="top-right" />
              <ComboBoxItem Content="Bottom Left" Tag="bottom-left" />
              <ComboBoxItem Content="Bottom" Tag="bottom" />
              <ComboBoxItem Content="Bottom Right" Tag="bottom-right" />
            </ComboBox>
            <CheckBox x:Name="OverlayDetailed" Content="Detailed Layout" Margin="0,0,0,10" />
            <CheckBox x:Name="OverlayFps" Content="FPS / AVG / MIN / 1% Low" Margin="0,0,0,8" />
            <CheckBox x:Name="OverlayCpu" Content="CPU" Margin="0,0,0,8" />
            <CheckBox x:Name="OverlayGpu" Content="GPU" Margin="0,0,0,8" />
            <CheckBox x:Name="OverlayMemory" Content="RAM / VRAM" Margin="0,0,0,8" />
            <CheckBox x:Name="OverlayFans" Content="Fan Speed" Margin="0,0,0,8" />
            <CheckBox x:Name="OverlayStorage" Content="NVMe" Margin="0,0,0,8" />
            <TextBlock Text="AVG / MIN / 1% Low: rolling 60 seconds. MIN is the slowest frame; 1% Low averages the slowest 1%." FontSize="11" Margin="0,0,0,10" />
            <Button x:Name="ResetFps" Content="Reset FPS" Margin="0,0,0,8" />
            <TextBlock x:Name="OverlayStatus" Text="FPS capture stopped" FontSize="11" />
          </StackPanel>
        </Expander>
        <Expander Header="Hardware Names" Foreground="#F0F5FA" Margin="0,16,0,0">
          <StackPanel>
            <TextBlock Text="Leave a name blank to use device information. Hover over a field to see its automatic name." FontSize="11" Foreground="#ADC1D2" Margin="0,10,0,8" />
            <TextBlock Text="SPD numbers identify sensor addresses, not physical slots. Installed slots are reported separately; assign a slot name only after confirming its sensor." FontSize="11" Foreground="#ADC1D2" Margin="0,0,0,12" />
            <StackPanel x:Name="NameFields" />
          </StackPanel>
        </Expander>
        <TextBlock Text="Changes save automatically." FontSize="11" Foreground="#ADC1D2" Margin="2,16,0,12" />
      </StackPanel>
    </ScrollViewer>
    <Grid x:Name="MonitorFooter" Grid.Row="2" Margin="14,4,20,10">
      <Button x:Name="Settings" HorizontalAlignment="Right" ToolTip="Settings" AutomationProperties.Name="Settings" Width="36" Height="36" Padding="0" Margin="8,0,0,0" />
    </Grid>
  </Grid>
</Window>
