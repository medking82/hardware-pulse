<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="Hardware Pulse Glass" Width="280" Height="650" MinWidth="240" MinHeight="340" WindowStyle="None" ResizeMode="CanResizeWithGrip" Background="#182332" Foreground="#F0F5FA" FontFamily="Segoe UI" FontSize="12" TextOptions.TextFormattingMode="Display" UseLayoutRounding="True" SnapsToDevicePixels="True" WindowStartupLocation="CenterScreen">
  <Window.Resources>
    <Style x:Key="PulseScrollThumb" TargetType="Thumb">
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Thumb">
            <Grid Background="Transparent">
              <Border x:Name="ThumbBody" Background="#607E94A5" CornerRadius="3" Margin="3,0" />
            </Grid>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="ThumbBody" Property="Background" Value="#B098B0C0" />
              </Trigger>
              <Trigger Property="IsDragging" Value="True">
                <Setter TargetName="ThumbBody" Property="Background" Value="#D0ADD9DF" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="PulseScrollPage" TargetType="RepeatButton">
      <Setter Property="Focusable" Value="False" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="RepeatButton">
            <Border Background="Transparent" />
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="ScrollBar">
      <Setter Property="Width" Value="12" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ScrollBar">
            <Grid Background="Transparent" Margin="0,3">
              <Track x:Name="PART_Track" IsDirectionReversed="True" Orientation="{TemplateBinding Orientation}" Minimum="{TemplateBinding Minimum}" Maximum="{TemplateBinding Maximum}" ViewportSize="{TemplateBinding ViewportSize}" Value="{Binding Value, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                <Track.DecreaseRepeatButton>
                  <RepeatButton Command="ScrollBar.PageUpCommand" Style="{StaticResource PulseScrollPage}" />
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                  <Thumb Style="{StaticResource PulseScrollThumb}" MinHeight="24" />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                  <RepeatButton Command="ScrollBar.PageDownCommand" Style="{StaticResource PulseScrollPage}" />
                </Track.IncreaseRepeatButton>
              </Track>
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="TextBlock">
      <Setter Property="TextWrapping" Value="Wrap" />
    </Style>
    <Style TargetType="RadioButton">
      <Setter Property="Foreground" Value="#F0F5FA"/><Setter Property="MinHeight" Value="36"/>
      <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="RadioButton">
        <Border x:Name="Focus" Background="Transparent" CornerRadius="8" Padding="6" BorderThickness="1" BorderBrush="Transparent">
          <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="26"/><ColumnDefinition/></Grid.ColumnDefinitions>
            <Border Width="18" Height="18" CornerRadius="9" BorderThickness="1.5" BorderBrush="{TemplateBinding Foreground}" VerticalAlignment="Center">
              <Ellipse x:Name="Dot" Width="8" Height="8" Fill="{TemplateBinding Foreground}" Visibility="Collapsed"/>
            </Border>
            <TextBlock Grid.Column="1" Text="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}" VerticalAlignment="Center"/>
          </Grid>
        </Border>
        <ControlTemplate.Triggers><Trigger Property="IsChecked" Value="True"><Setter TargetName="Dot" Property="Visibility" Value="Visible"/></Trigger><Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="Focus" Property="BorderBrush" Value="{Binding Foreground,RelativeSource={RelativeSource TemplatedParent}}"/></Trigger><Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.5"/></Trigger></ControlTemplate.Triggers>
      </ControlTemplate></Setter.Value></Setter>
    </Style>
    <Style x:Key="SliderPage" TargetType="RepeatButton"><Setter Property="Focusable" Value="False"/><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="RepeatButton"><Border Height="4" CornerRadius="2" Background="{TemplateBinding Background}"/></ControlTemplate></Setter.Value></Setter></Style>
    <Style TargetType="Slider">
      <Setter Property="Foreground" Value="#A5E7D5"/><Setter Property="MinHeight" Value="36"/>
      <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Slider">
        <Grid Margin="8,0"><Track x:Name="PART_Track" Minimum="{TemplateBinding Minimum}" Maximum="{TemplateBinding Maximum}" Value="{Binding Value,RelativeSource={RelativeSource TemplatedParent},Mode=TwoWay}">
          <Track.DecreaseRepeatButton><RepeatButton Command="Slider.DecreaseLarge" Background="{TemplateBinding Foreground}" Style="{StaticResource SliderPage}"/></Track.DecreaseRepeatButton>
          <Track.Thumb><Thumb Width="18" Height="18"><Thumb.Template><ControlTemplate TargetType="Thumb"><Border x:Name="Knob" Background="#F5F7FA" BorderBrush="#718596" BorderThickness="1" CornerRadius="9"/><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Knob" Property="BorderThickness" Value="2"/></Trigger><Trigger Property="IsDragging" Value="True"><Setter TargetName="Knob" Property="Background" Value="#A5E7D5"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Thumb.Template></Thumb></Track.Thumb>
          <Track.IncreaseRepeatButton><RepeatButton Command="Slider.IncreaseLarge" Background="#718596" Style="{StaticResource SliderPage}"/></Track.IncreaseRepeatButton>
        </Track></Grid>
        <ControlTemplate.Triggers><Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.45"/></Trigger></ControlTemplate.Triggers>
      </ControlTemplate></Setter.Value></Setter>
    </Style>
    <Style TargetType="Button">
      <Setter Property="FocusVisualStyle">
        <Setter.Value>
          <Style TargetType="Control">
            <Setter Property="Template">
              <Setter.Value>
                <ControlTemplate>
                  <Border Margin="-3" CornerRadius="18" BorderThickness="1" BorderBrush="#BFEAF9" />
                </ControlTemplate>
              </Setter.Value>
            </Setter>
          </Style>
        </Setter.Value>
      </Setter>
      <Setter Property="Foreground" Value="#F3F7FC" />
      <Setter Property="Background" Value="#223B536B" />
      <Setter Property="Padding" Value="10,5" />
      <Setter Property="Margin" Value="0,0,6,0" />
      <Setter Property="MinHeight" Value="28" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Plate" Background="{TemplateBinding Background}" BorderBrush="#496F829A" BorderThickness="1" CornerRadius="15" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Plate" Property="Background" Value="#505E829D" />
              </Trigger>
              <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter TargetName="Plate" Property="BorderBrush" Value="#BFEAF9" />
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Plate" Property="Background" Value="#8078A5B9" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="CheckBox">
      <Setter Property="Foreground" Value="#D9E7F1" />
      <Setter Property="VerticalContentAlignment" Value="Center" />
      <Setter Property="Padding" Value="3" />
      <Setter Property="Margin" Value="0,0,10,0" />
      <Setter Property="MinHeight" Value="32" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="CheckBox">
            <Grid Background="Transparent">
              <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="44" /></Grid.ColumnDefinitions>
              <TextBlock Text="{TemplateBinding Content}" Margin="0,4,10,4" VerticalAlignment="Center" TextWrapping="Wrap" />
              <Border x:Name="SwitchTrack" Grid.Column="1" Width="38" Height="22" CornerRadius="11" Background="#52606D" BorderBrush="#8797A6" BorderThickness="1" VerticalAlignment="Center">
                <Ellipse x:Name="SwitchKnob" Width="16" Height="16" Fill="#F0F5FA" HorizontalAlignment="Left" Margin="2,0" />
              </Border>
            </Grid>
            <ControlTemplate.Triggers>
              <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="SwitchTrack" Property="Background" Value="#A6D8C8" />
                <Setter TargetName="SwitchKnob" Property="Fill" Value="#182332" />
                <Setter TargetName="SwitchKnob" Property="HorizontalAlignment" Value="Right" />
              </Trigger>
              <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="SwitchTrack" Property="BorderBrush" Value="#FFFFFF" /><Setter TargetName="SwitchTrack" Property="BorderThickness" Value="2" /></Trigger>
              <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.5" /></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="Expander">
      <Setter Property="Foreground" Value="#F0F5FA" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Expander">
            <Border BorderBrush="#486B7C89" BorderThickness="0,0,0,1" Padding="0,0,0,8">
              <StackPanel>
                <ToggleButton x:Name="SectionHeader" IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" Foreground="{TemplateBinding Foreground}" MinHeight="40" HorizontalContentAlignment="Stretch">
                  <ToggleButton.Template>
                    <ControlTemplate TargetType="ToggleButton">
                      <Border x:Name="HeaderPlate" Background="Transparent" CornerRadius="8" Padding="8,6">
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="20" /></Grid.ColumnDefinitions>
                          <ContentPresenter VerticalAlignment="Center" />
                          <Path x:Name="Chevron" Grid.Column="1" Data="M 0 0 L 5 5 L 10 0" Stroke="{TemplateBinding Foreground}" StrokeThickness="1.5" HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5" />
                        </Grid>
                      </Border>
                      <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True"><Setter TargetName="Chevron" Property="RenderTransform"><Setter.Value><RotateTransform Angle="180" /></Setter.Value></Setter></Trigger>
                        <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="HeaderPlate" Property="Background" Value="#185F8799" /></Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="HeaderPlate" Property="BorderBrush" Value="#BFEAF9" /><Setter TargetName="HeaderPlate" Property="BorderThickness" Value="1" /></Trigger>
                      </ControlTemplate.Triggers>
                    </ControlTemplate>
                  </ToggleButton.Template>
                  <ContentPresenter Content="{TemplateBinding Header}" TextElement.FontWeight="SemiBold" />
                </ToggleButton>
                <ContentPresenter x:Name="SectionBody" Content="{TemplateBinding Content}" Visibility="Collapsed" Margin="8,10,8,8" />
              </StackPanel>
            </Border>
            <ControlTemplate.Triggers><Trigger Property="IsExpanded" Value="True"><Setter TargetName="SectionBody" Property="Visibility" Value="Visible" /></Trigger></ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>
  <Grid x:Name="Viewport">
    <Grid.Background>
      <RadialGradientBrush Center="0.1,0" GradientOrigin="0,0" RadiusX="1.3" RadiusY="1">
        <GradientStop Color="#404C9DAD" Offset="0" />
        <GradientStop Color="#05152136" Offset="0.6" />
        <GradientStop Color="#302C3C68" Offset="1" />
      </RadialGradientBrush>
    </Grid.Background>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <StackPanel Margin="14,10,14,8">
      <Grid>
        <Border x:Name="DragHandle" Background="Transparent" Margin="0,0,90,0" Cursor="SizeAll">
          <StackPanel Orientation="Horizontal">
            <ContentControl x:Name="BrandIcon" VerticalAlignment="Center" Margin="0,0,7,0" />
            <TextBlock Text="Pulse" FontSize="22" FontWeight="SemiBold" />
          </StackPanel>
        </Border>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
          <Button x:Name="Minimize" Content="−" ToolTip="Minimize" Padding="10,3" />
          <Button x:Name="Close" Content="×" ToolTip="Close widget" Padding="10,3" Margin="0" />
        </StackPanel>
      </Grid>
      <WrapPanel x:Name="MonitorControls" Orientation="Horizontal" Margin="0,10,0,8">
        <Button x:Name="Live" Content="Live" />
        <Button x:Name="Max" Content="Session Max" />
        <Button x:Name="Details" Content="Details" ToolTip="Show full hardware details" Padding="7,5" Margin="0" />
        <Button x:Name="DesktopQuick" Content="Desktop" ToolTip="Show Desktop View" Padding="7,5" Margin="6,0,0,0" />
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0,0,0">
          <ContentControl x:Name="FpsQuickIcon" Width="16" Height="16" Margin="0,0,5,0" VerticalAlignment="Center" IsHitTestVisible="False" />
          <CheckBox x:Name="FpsQuick" Content="FPS" ToolTip="Enable Game Overlay" VerticalAlignment="Center" />
        </StackPanel>
      </WrapPanel>
      <TextBlock x:Name="Status" Text="Connecting to sensors…" Foreground="#ACDFD2" FontSize="10" />
    </StackPanel>
    <ScrollViewer x:Name="CardScroll" Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="12,0,12,0" FocusVisualStyle="{x:Null}">
      <ScrollViewer.Template>
        <ControlTemplate TargetType="ScrollViewer">
          <Border x:Name="ScrollFocus" BorderThickness="1" BorderBrush="Transparent" CornerRadius="14">
            <Grid>
              <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" Margin="12,0,24,0" CanContentScroll="{TemplateBinding CanContentScroll}" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" />
              <ScrollBar x:Name="PART_VerticalScrollBar" HorizontalAlignment="Right" Width="10" Orientation="Vertical" Minimum="0" Maximum="{TemplateBinding ScrollableHeight}" ViewportSize="{TemplateBinding ViewportHeight}" Value="{Binding VerticalOffset, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}" Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" />
            </Grid>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="ScrollFocus" Property="BorderBrush" Value="#608ECBC4" /></Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </ScrollViewer.Template>
      <StackPanel><StackPanel x:Name="Cards" /><StackPanel x:Name="QuotaCards" /></StackPanel>
    </ScrollViewer>
    <TextBlock x:Name="CardsEmpty" Grid.Row="1" Text="No cards shown. Choose cards in Settings." Visibility="Collapsed" Margin="20" HorizontalAlignment="Center" VerticalAlignment="Center" />
    <Border x:Name="SettingsToolbar" Grid.Row="1" Visibility="Collapsed" VerticalAlignment="Top" Padding="18,8" Height="56">
        <StackPanel Orientation="Horizontal">
          <Button x:Name="Back" ToolTip="Back to Monitor" AutomationProperties.Name="Back to Monitor" Width="32" Padding="7" />
          <TextBlock Text="Settings" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center" Margin="4,0,0,0" />
        </StackPanel>
    </Border>
    <ScrollViewer x:Name="SettingsPage" Grid.Row="1" Margin="0,56,0,0" Visibility="Collapsed" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="14,10,14,0" FontSize="14" FocusVisualStyle="{x:Null}">
      <StackPanel MaxWidth="620" HorizontalAlignment="Stretch">
        <Expander Header="General" IsExpanded="True" Margin="0,0,0,10">
          <StackPanel>
            <TextBlock Text="Language" Margin="0,0,0,6" />
            <ComboBox x:Name="LanguagePicker" AutomationProperties.Name="Language" MinHeight="30" Margin="0,0,0,16" Foreground="#182332" FontFamily="Segoe UI, Microsoft YaHei UI, Microsoft JhengHei UI">
              <ComboBoxItem Tag="auto" Content="Auto (System)" />
              <ComboBoxItem Tag="en" Content="English" />
              <ComboBoxItem Tag="zh-CN" Content="简体中文" />
              <ComboBoxItem Tag="zh-TW" Content="繁體中文" />
            </ComboBox>
            <CheckBox x:Name="StartWithWindows" Content="Start with Windows" Margin="0,0,0,10" />
            <TextBlock x:Name="StartupStatus" FontSize="11" Margin="0,0,0,10" />
            <TextBlock Text="Version 0.6.8 · Marck Wong" FontSize="11" Foreground="#ADC1D2" Margin="0,0,0,8" />
            <Button x:Name="GitHub" Content="GitHub" HorizontalAlignment="Left" ToolTip="github.com/medking82/hardware-pulse" />
            <CheckBox x:Name="AutoUpdates" Content="Check for Updates Automatically" Margin="0,12,0,8" />
            <CheckBox x:Name="AutoDownload" Content="Download Updates Automatically" Margin="0,0,0,8" />
            <TextBlock Text="Checks GitHub for new releases. No hardware data is sent." FontSize="11" Margin="0,0,0,8" />
            <Button x:Name="CheckUpdates" Content="Check for Updates" Margin="0,0,0,8" />
            <Button x:Name="GetUpdate" Content="Download Update" Visibility="Collapsed" Margin="0,0,0,8" />
            <ProgressBar x:Name="DownloadProgress" Minimum="0" Maximum="100" Height="4" Visibility="Collapsed" Margin="0,0,0,8" />
            <Button x:Name="InstallUpdate" Content="Install and Restart" Visibility="Collapsed" Margin="0,0,0,8" />
            <TextBlock x:Name="UpdateStatus" Text="" FontSize="11" />
            <TextBlock Text="Sensors by LibreHardwareMonitor. Shared driver by PawnIO." FontSize="10" Foreground="#ADC1D2" Margin="0,10,0,0" />
          </StackPanel>
        </Expander>
        <Expander x:Name="AppearanceSection" Header="App Appearance" IsExpanded="False" Margin="0,0,0,10">
          <StackPanel>
            <Grid Margin="0,0,0,8">
              <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <TextBlock Text="Background Opacity" Margin="0,0,8,0" />
              <TextBlock x:Name="OpacityValue" Grid.Column="1" Text="70%" />
            </Grid>
            <Slider x:Name="OpacitySlider" Minimum="0" Maximum="100" Value="70" SmallChange="5" LargeChange="10" AutomationProperties.Name="Background opacity" Margin="0,0,0,18" />
            <CheckBox x:Name="Solid" Content="Solid Background" Margin="0,0,0,14" />
            <CheckBox x:Name="Large" Content="Larger Text" Visibility="Collapsed" Margin="0" />
            <Grid Margin="0,0,0,6">
              <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
              <TextBlock Text="Text Size"/>
              <TextBlock x:Name="FontSizeValue" Grid.Column="1" Text="12 px"/>
            </Grid>
            <Slider x:Name="FontSizeSlider" Minimum="10" Maximum="16" Value="12" TickFrequency="1" IsSnapToTickEnabled="True" SmallChange="1" LargeChange="1" AutomationProperties.Name="Text Size"/>
            <TextBlock Text="Logical pixels; follows Windows display scaling." FontSize="11" TextWrapping="Wrap" Margin="0,6,0,0"/>
            <Button x:Name="BackgroundColor" Content="Background Color" Margin="0,14,0,0" />
            <TextBlock Text="Text contrast adapts to the selected color. Glass blur is controlled by Windows; Solid Background disables it." FontSize="11" Margin="0,8,0,0" />
            <TextBlock Text="Colors" FontWeight="SemiBold" Margin="0,18,0,8" />
            <RadioButton x:Name="HardwareReadingColors" GroupName="ReadingColors" Content="Hardware Colors" Margin="0,0,0,10" />
            <RadioButton x:Name="UnifiedReadingColors" GroupName="ReadingColors" Content="Unified Color" Margin="0,0,0,8" />
            <Button x:Name="ReadingColorPicker" Content="#DDE9F0" AutomationProperties.Name="Unified Color" Margin="0,0,0,8" />
            <TextBlock Text="Monitor icons and temperatures share this palette. Labels stay neutral. Desktop Auto Contrast is independent." FontSize="11" />
          </StackPanel>
        </Expander>
        <Expander x:Name="DesktopAppearanceSection" Header="Desktop Appearance" Margin="0,0,0,10"><StackPanel>
            <Button x:Name="DesktopRecommended" Content="Use Recommended Style" Margin="0,0,0,10" />
            <CheckBox x:Name="DesktopAppIconColors" Content="Icons follow App colors" Margin="0,0,0,8" />
            <TextBlock Text="When off, icons match Desktop text color." FontSize="11" Margin="0,0,0,12" />
            <StackPanel Margin="0,8,0,0">
            <DockPanel><TextBlock x:Name="DesktopFontValue" DockPanel.Dock="Right" /><TextBlock Text="Font Size" /></DockPanel>
            <Slider x:Name="DesktopFontSize" Minimum="10" Maximum="32" TickFrequency="1" IsSnapToTickEnabled="True" AutomationProperties.Name="Desktop Font Size" Margin="0,8,0,12" />
            <DockPanel><TextBlock x:Name="DesktopSpacingValue" DockPanel.Dock="Right" /><TextBlock Text="Row Spacing" /></DockPanel>
            <Slider x:Name="DesktopSpacing" Minimum="4" Maximum="40" TickFrequency="1" IsSnapToTickEnabled="True" AutomationProperties.Name="Row Spacing" Margin="0,8,0,12" />
            <CheckBox x:Name="DesktopAutoContrast" Content="Auto Contrast" Margin="0,0,0,8" />
            <DockPanel><TextBlock x:Name="DesktopBackgroundOpacityValue" DockPanel.Dock="Right" /><TextBlock Text="Desktop background opacity" /></DockPanel>
            <Slider x:Name="DesktopBackgroundOpacity" Minimum="0" Maximum="100" TickFrequency="1" IsSnapToTickEnabled="True" AutomationProperties.Name="Desktop background opacity" Margin="0,8,0,8" />
            <DockPanel><TextBlock x:Name="DesktopOverlayOpacityValue" DockPanel.Dock="Right" /><TextBlock Text="Topmost background opacity" /></DockPanel>
            <Slider x:Name="DesktopOverlayOpacity" Minimum="30" Maximum="90" TickFrequency="1" IsSnapToTickEnabled="True" AutomationProperties.Name="Topmost background opacity" Margin="0,8,0,8" />
            <TextBlock Text="Background opacity is separate from text. Topmost uses its own background setting. Desktop blur is not available." TextWrapping="Wrap" Margin="0,0,0,12" />
            <TextBlock Text="Adapts text and a single rounded backdrop to keep readings clear." FontSize="11" Margin="0,0,0,12" />
            <DockPanel><TextBlock x:Name="DesktopTextOpacityValue" DockPanel.Dock="Right" /><TextBlock Text="Text Opacity" /></DockPanel>
            <Slider x:Name="DesktopTextOpacity" Minimum="30" Maximum="100" TickFrequency="1" IsSnapToTickEnabled="True" AutomationProperties.Name="Text Opacity" Margin="0,8,0,8" />
            <TextBlock Text="Auto Contrast uses a readable panel. Turn it off for transparent text and custom colors." FontSize="11" Margin="0,0,0,12" />
            <TextBlock Text="Text Color" Margin="0,0,0,6" />
            <Button x:Name="DesktopColor" Content="#E4F3EF" AutomationProperties.Name="Desktop Text Color" Margin="0,0,0,12" />
            </StackPanel>

          </StackPanel>
        </Expander>
        <Expander Header="Window" IsExpanded="False" Margin="0,0,0,10">
          <StackPanel>
            <CheckBox x:Name="Pin" Content="Always on Top" Margin="0,0,0,10" />
            <CheckBox x:Name="LockPosition" Content="Lock Position and Size" Margin="0,0,0,10" />
            <TextBlock Text="Position and size are remembered automatically." FontSize="11" Foreground="#ADC1D2" />
          </StackPanel>
        </Expander>
        <Expander Header="App Cards" Foreground="#F0F5FA" Margin="0,16,0,0">
            <StackPanel>
            <StackPanel x:Name="CardOptions" Margin="0,10,0,0" />
            <TextBlock Text="Network Speed Unit" Margin="0,14,0,6" />
            <ComboBox x:Name="NetworkUnit" SelectedIndex="0" AutomationProperties.Name="Network Speed Unit">
              <ComboBoxItem Tag="auto" Content="Auto" /><ComboBoxItem Tag="KB/s" Content="KB/s" /><ComboBoxItem Tag="MB/s" Content="MB/s" /><ComboBoxItem Tag="Mbit/s" Content="Mbit/s" />
            </ComboBox>
            <TextBlock Text="LAN and Wi-Fi link speeds update independently. Download and upload refer to the traffic adapter. Link speed is not Internet speed." FontSize="11" Margin="0,8,0,0" />
            </StackPanel>
        </Expander>
        <Expander Header="AI Quota" Foreground="#F0F5FA" Margin="0,16,0,0">
          <StackPanel>
            <TextBlock Text="Read remaining quota from existing local logins. Refreshes every 5 minutes." TextWrapping="Wrap" Margin="0,0,0,10" />
            <CheckBox x:Name="QuotaCodex" Content="Codex" Margin="0,6" />
            <CheckBox x:Name="QuotaAntigravity" Content="Antigravity" Margin="0,6" />
            <CheckBox x:Name="QuotaClaude" Content="Claude" Margin="0,6" />
            <TextBlock Text="App quota display" Margin="0,12,0,6" />
            <ComboBox x:Name="QuotaDisplay" AutomationProperties.Name="App quota display"><ComboBoxItem Content="Essential quotas"/><ComboBoxItem Content="All available quotas"/></ComboBox>
            <TextBlock Text="Desktop keeps its essential quotas. Drag the handles to reorder the three provider cards." FontSize="12" Opacity="0.8" Margin="0,8,0,0" />
            <Button x:Name="QuotaRefresh" Content="Refresh Quota" Margin="0,12,0,8" />
            <TextBlock Text="Login in Codex or Claude Code first. Keep Antigravity open. Pulse never changes your login." TextWrapping="Wrap" Opacity="0.8" />
          </StackPanel>
        </Expander>
        <Expander Header="Game Overlay" Foreground="#F0F5FA" Margin="0,16,0,0">
          <StackPanel Margin="0,10,0,0">
            <TextBlock Text="Windowed / borderless apps. Auto follows the foreground app; manual selection reconnects after restart. FPS uses the installed collector and application presents, not generated frames." FontSize="11" Margin="0,0,0,10" />
            <ComboBox x:Name="GamePicker" AutomationProperties.Name="Game" MinHeight="30" Margin="0,0,0,8" Foreground="#182332" />
            <Button x:Name="RefreshGames" Content="Refresh Games" Margin="0,0,0,10" />
            <CheckBox x:Name="OverlayEnabled" Content="Show Overlay" Margin="0,0,0,10" />
            <ComboBox x:Name="OverlayPosition" AutomationProperties.Name="Overlay Position" MinHeight="30" Margin="0,0,0,10" Foreground="#182332">
              <ComboBoxItem Content="Top Left" Tag="top-left" />
              <ComboBoxItem Content="Top" Tag="top" />
              <ComboBoxItem Content="Top Right" Tag="top-right" />
              <ComboBoxItem Content="Bottom Left" Tag="bottom-left" />
              <ComboBoxItem Content="Bottom" Tag="bottom" />
              <ComboBoxItem Content="Bottom Right" Tag="bottom-right" />
            </ComboBox>
            <CheckBox x:Name="OverlayDetailed" Content="Detailed Layout" Margin="0,0,0,10" />
            <TextBlock Text="Overlay Background Color" Margin="0,4,0,6" />
            <Button x:Name="OverlayBackground" Content="#111923" AutomationProperties.Name="Overlay Background Color" Margin="0,0,0,10" />
            <DockPanel Margin="0,0,0,6">
              <TextBlock x:Name="OverlayOpacityValue" Text="80%" DockPanel.Dock="Right" />
              <TextBlock Text="Background Opacity" />
            </DockPanel>
            <Slider x:Name="OverlayOpacity" Minimum="0" Maximum="100" TickFrequency="1" IsSnapToTickEnabled="True" AutomationProperties.Name="Background Opacity" Margin="0,0,0,8" />
            <TextBlock Text="0% transparent · 100% opaque. Text stays visible." FontSize="11" Margin="0,0,0,8" />
            <TextBlock Text="Preview (sample data)" FontSize="11" Margin="0,0,0,6" />
            <Border x:Name="OverlayPreview" CornerRadius="8" Padding="12,8" Margin="0,0,0,8">
              <TextBlock x:Name="OverlayPreviewText" Text="FPS 60 · GPU 52°C" Foreground="White" FontSize="14" />
            </Border>
            <Button x:Name="ResetOverlayAppearance" Content="Reset Overlay Appearance" Margin="0,0,0,12" />
            <CheckBox x:Name="OverlayFps" Content="FPS / AVG / MIN / 1% Low" Margin="0,0,0,8" />
            <CheckBox x:Name="OverlayCpu" Content="CPU" Margin="0,0,0,8" />
            <CheckBox x:Name="OverlayGpu" Content="GPU" Margin="0,0,0,8" />
            <CheckBox x:Name="OverlayMemory" Content="RAM / VRAM" Margin="0,0,0,8" />
            <CheckBox x:Name="OverlayFans" Content="Fan Speed" Margin="0,0,0,8" />
            <CheckBox x:Name="OverlayStorage" Content="NVMe" Margin="0,0,0,8" />
            <TextBlock Text="AVG / MIN / 1% Low: rolling 60 seconds. MIN is the slowest frame; 1% Low averages the slowest 1%." FontSize="11" Margin="0,0,0,10" />
            <Button x:Name="ResetFps" Content="Reset FPS" Margin="0,0,0,8" />
            <TextBlock x:Name="OverlayStatus" Text="FPS capture stopped" FontSize="11" />
          </StackPanel>
        </Expander>
        <Expander x:Name="DesktopSection" Header="Desktop Layout" Foreground="#F0F5FA" Margin="0,16,0,0">
          <StackPanel>
            <TextBlock Text="Readings on your wallpaper, below other apps. Uses the same collector." FontSize="11" Margin="0,10,0,12" />
            <CheckBox x:Name="DesktopEnabled" Content="Desktop Mode" Margin="0,0,0,10" />
            <CheckBox x:Name="DesktopAlwaysOnTop" Content="Always on top" Margin="0,0,0,10" />
            <TextBlock Text="Show above apps. Locked panels let mouse input pass through. Exclusive fullscreen may hide the panel." TextWrapping="Wrap" Margin="0,0,0,12" />
            <TextBlock Text="Select Edit Desktop, then move or resize the panel. Lock it directly on the desktop." FontSize="11" Margin="0,0,0,12" />
            <CheckBox x:Name="DesktopLocked" Visibility="Collapsed" Content="Lock Desktop" Margin="0,0,0,10" />
            <Button x:Name="DesktopMove" Content="Edit Desktop Position" Margin="0,0,0,8" />
            <Button x:Name="DesktopDone" Visibility="Collapsed" Content="Show Desktop View" Margin="0,0,0,8" />
            <TextBlock Text="Edges snap while moving. Drag away to release; Alt temporarily bypasses snapping." FontSize="11" Margin="0,0,0,12" />
            <TextBlock Text="Columns" Margin="0,8,0,6" />
            <ComboBox x:Name="DesktopColumns" Margin="0,0,0,12" AutomationProperties.Name="Desktop Columns"><ComboBoxItem Content="Auto columns"/><ComboBoxItem Content="1 column"/><ComboBoxItem Content="2 columns"/><ComboBoxItem Content="3 columns"/></ComboBox>
            <Button x:Name="DesktopResetPosition" Content="Reset Desktop Position" Margin="0,0,0,8" />
            <TextBlock x:Name="DesktopStatus" Text="Use the system tray to edit or exit Desktop Mode." FontSize="11" />
          </StackPanel>
        </Expander>
        <Expander Header="Desktop Readings" Margin="0,16,0,0"><StackPanel>            <TextBlock Text="Reading Order" FontWeight="SemiBold" Margin="0,8,0,8" />
            <TextBlock Text="Drag the handle or use Up / Down keys. Esc cancels a drag. Unavailable readings retain their position." FontSize="11" Margin="0,0,0,8" />
            <StackPanel x:Name="DesktopOrderList" Margin="0,0,0,12" />
</StackPanel></Expander>
        <Expander Header="Hardware Names" Foreground="#F0F5FA" Margin="0,16,0,0">
          <StackPanel>
            <TextBlock Text="Leave a name blank to use device information. Hover over a field to see its automatic name." FontSize="11" Foreground="#ADC1D2" Margin="0,10,0,8" />
            <TextBlock Text="SPD numbers identify sensor addresses, not physical slots. Installed slots are reported separately; assign a slot name only after confirming its sensor." FontSize="11" Foreground="#ADC1D2" Margin="0,0,0,12" />
            <StackPanel x:Name="NameFields" />
          </StackPanel>
        </Expander>
        <TextBlock Text="Changes save automatically." FontSize="11" Foreground="#ADC1D2" Margin="2,16,0,12" />
      </StackPanel>
    </ScrollViewer>
    <Grid x:Name="MonitorFooter" Grid.Row="2" Margin="14,4,20,10">
      <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
      <Button x:Name="HomeUpdate" Grid.Column="0" Visibility="Collapsed" HorizontalAlignment="Right" VerticalAlignment="Center" MinHeight="36" Margin="0" Padding="10,6" FontWeight="SemiBold" />
      <Button x:Name="Settings" Grid.Column="1" HorizontalAlignment="Right" ToolTip="Settings" AutomationProperties.Name="Settings" Width="36" Height="36" Padding="0" Margin="8,0,0,0" />
    </Grid>
  </Grid>
</Window>
